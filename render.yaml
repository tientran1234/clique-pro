services:
  - type: web
    name: clique-backend
    runtime: node
    region: singapore
    plan: free
    buildCommand: pnpm install && pnpm prisma generate && pnpm build
    startCommand: sh start.sh
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        # Internal URL - faster for services on Render
        value: postgresql://clique_use:STHIKzYmvZgpwd8ttdgFwmdSUFCPRRiw@dpg-d6dbrhi4d50c73amchug-a/clique_dating
      - key: ALLOWED_ORIGINS
        value: https://clique-fe.vercel.app,http://localhost:5173
